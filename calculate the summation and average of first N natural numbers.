#include<stdio.h>

int main(){

int n,i,sum = 0;

printf("Enter The Number:");
scanf("%d",&n);

for(i=1;i<=n;i++){
	sum = sum+i;
}
int avg = sum/n;

printf("Sum of The Number %d: \n",sum);
printf("Avg of The Number %d: \n",avg);
	
	return 0;
}
