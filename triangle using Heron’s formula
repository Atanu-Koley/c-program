#include<stdio.h>
#include<math.h>
int main(){
 
 int a,b,c,s;
 printf("Enter The number \n=");
 scanf("%d%d%d",&a,&b,&c);
 
 s=((a+b+c)/2);
 int Area = sqrt(s*(s-a)*(s-b)*(s-c));
 
 printf("Area = %d\n ",Area);
	
	return 0;
}
